import 'package:flutter/material.dart';

// Vellionation brand -- nature-inspired dark theme
class PulseFlowColors {
  static const background     = Color(0xFF060A08); // near-black forest
  static const surface        = Color(0xFF0C120F); // dark green card
  static const surfaceDialog  = Color(0xFF101814); // dialog overlay
  static const primary        = Color(0xFF22A069); // emerald green
  static const primaryLight   = Color(0xFFDFECE6); // light mint
  static const secondary      = Color(0xFFF6A655); // vibrant orange
  static const accent         = Color(0xFF53C6B3); // teal/mint
  static const foreground     = Color(0xFFDFECE6); // light mint text
  static const mutedForeground= Color(0xFF8AA899); // muted green-grey

  // Legacy aliases — keeps existing widget code working
  static const deepPurple = background;
  static const paleGold   = primary;
}
